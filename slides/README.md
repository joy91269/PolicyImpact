# Presentation artifacts

- `Jiuyi_Zheng_PolicyImpact_Presentation.pptx` — editable six-slide deck with
  speaker notes on every slide.
- `Jiuyi_Zheng_PolicyImpact_Presentation.pdf` — visually faithful convenience
  copy.

The deck uses only the controlled fixture results and explicitly distinguishes
them from live-model or production performance. The PPTX was rendered and
checked for overflow; both final formats were visually reviewed page by page.

To rebuild, use the artifact-tool source under `artifact_tool_workspace/`, render
the PPTX, then assemble the verified renders with `scripts/build_slide_pdf.py`.
