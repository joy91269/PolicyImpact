# Video package

The repository includes a timed script, an exact demo runbook, and a recording
checklist. The final recording is intentionally absent because Jiuyi Zheng must
appear on camera.

Required final filename:

`video/Jiuyi_Zheng_PolicyImpact_Demo.mp4`

Use the reviewed offline-fixture mode during recording. It is deterministic,
works without network access, and is visibly labeled in the application.
